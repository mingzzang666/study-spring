<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mysql.mapper.MemberMapper">
    <insert id="join" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_member (member_email, member_password, member_name, member_status)
        values (#{memberEmail}, #{memberPassword}, #{memberName}, #{memberStatus})
    </insert>

    <select id="login">
        select id, member_email, member_password, member_name, member_status, created_date, updated_date
        from tbl_member
        where member_email = #{memberEmail} and member_password = #{memberPassword}
    </select>

    <select id="findInformationByEmail">
        select id, member_email, member_password, member_name, created_date, updated_date
        from tbl_member
        where member_email = #{memberEmail}
    </select>

    <select id="selectCountByMemberEmail">
        select count(*) from tbl_member
        where member_email = #{memberEmail}
    </select>

    <select id="selectAllMember">
        select * from tbl_member
    </select>
</mapper>