<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.threetier.mapper.MemberMapper">
    <insert id="insertMember">
        insert into tbl_member (member_email, member_password, member_name)
        values (#{memberEmail}, #{memberPassword}, #{memberName})
    </insert>

    <insert id="insertKakaoMember">
        insert into tbl_member (kakao_email, kakao_profile_url, provider, member_name)
        values (#{kakaoEmail}, #{kakaoProfileUrl}, #{provider}, #{memberName})
    </insert>

    <select id="selectMemberForLogin">
        select id, member_email, member_name, created_datetime, updated_datetime
        from tbl_member
        where member_email = #{memberEmail} and member_password = #{memberPassword} and member_status = 'active'
    </select>

    <select id="selectMemberByKakaoEmail">
        select id, member_email, member_name, member_status,
               kakao_email, kakao_profile_url, provider, created_datetime, updated_datetime
        from tbl_member
        where kakao_email = #{kakaoEmail}
    </select>

</mapper>